export enum PostStatus {
    PUBLISHED = 'published',
    DRAFT = 'draft',
    DELETED = 'deleted',
}

export enum PostTags {
    FEATURED = 'featured',
    HOT = 'hot',
    NEW = 'new',
    URGENT = 'urgent',
}
